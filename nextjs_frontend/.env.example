# Environment variables for Next.js frontend (client-safe only)
# PUBLIC_INTERFACE
# NEXT_PUBLIC_API_BASE_URL: Base URL for FastAPI backend (e.g., https://api.example.com)
NEXT_PUBLIC_API_BASE_URL=
# Optional toggles already used in app
NEXT_PUBLIC_COOKIE_BANNER_ENABLED=true
NEXT_PUBLIC_CONSENT_REQUIRED=false
