# Environment variables for Next.js frontend (client-safe only)

# Port (used by Next.js CLI). If unset, Next defaults to 3000.
PORT=3000

# Base URL for external backend API. Leave empty to use internal Next.js /api routes.
# Note: Only NEXT_PUBLIC_* variables are exposed to the browser.
NEXT_PUBLIC_API_BASE=""

# Optional toggles already used in app
# Show cookie banner on load
NEXT_PUBLIC_COOKIE_BANNER_ENABLED="false"
# Require consent modal before using certain features
NEXT_PUBLIC_CONSENT_REQUIRED="false"
